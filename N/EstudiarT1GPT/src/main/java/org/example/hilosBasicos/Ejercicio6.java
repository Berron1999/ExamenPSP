package org.example.hilosBasicos;
/*Ejercicio 6 — Banco simple: synchronized y sleep

Objetivo: proteger estado compartido con métodos sincronizados.

Clase Cuenta con saldo, ingresar(int), retirar(int).

Lanza N hilos que hacen operaciones aleatorias (±1..±5), 1000 veces.

Protege con synchronized y verifica que el saldo final sea el esperado.

Reto: si usas retirar que no permita saldo negativo, añade if (saldo >= x) y comprueba que no aparece ninguna inconsistencia.*/
public class Ejercicio6 {
}
